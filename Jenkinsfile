pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                bat 'mvn clean install'
            }
        }

        // Add other stages as needed
    }

    // Add post actions and other pipeline configuration
}
